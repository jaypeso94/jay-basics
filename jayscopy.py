#Practice

#asd = {"message", "tag", "framework"}
#vwv = {"message", "metadata", "token", "ping", "nack"}
#weve = {"metadata", "tag", "token"}
#ewr = {"framework", "message", "echo", "token"}
#set_list = [asd, vwv, weve, ewr]

#def sort_list_of_sets(set_list):
#	sorted_list = sort_list_of_sets(set_list)

#print(sorted_list)


for i in range(len(set_list)):
        set_1 = set_list[i]
    if set_list[i] > set_list[i + 1]:

